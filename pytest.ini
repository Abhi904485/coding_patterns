[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

addopts =
    -ra
    -q
    --cov=src
    --cov-branch
    --cov-report=term-missing
    --cov-report=xml
    --cov-report=html
    --cov-append
    --color=yes
